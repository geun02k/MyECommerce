# Application Configuration - Plain Text Version
# These values must be encrypted using Jasypt before being committed to the repository.

# MySQL DB Connection info
spring.datasource.url=jdbc:mysql://localhost:3306/zero_my_e_commerce?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
spring.datasource.username=myecommerce_user
spring.datasource.password=zerobase

# JWT token secret key
spring.jwt.secret-key=my-spring-boot-my-e-commerce-project-jwt-secret-key

